/**
 * Vue Mapping Odds — correction manuelle du rattachement Box Type ↔ set d'odds.
 *
 * Squelette : le contenu est implémenté dans WS4. La vue est déjà enregistrée
 * dans ViewTabs / App pour que la navigation et le build soient cohérents.
 */
export function OddsMappingView() {
  return null
}
